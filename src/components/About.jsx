import styles from "./About.module.css";

export default function () {
  return (
    <div>
      <h2 className={styles.about}>
        Fun fact about us is that we are everywhere!
      </h2>
    </div>
  );
}
